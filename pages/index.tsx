import { Box } from '@chakra-ui/react';

import FolderInput from '@/domains/components/FolderInput';

const Home = () => {
  return (
    <Box px={7} py={10}>
      <FolderInput />
    </Box>
  );
};

export default Home;
